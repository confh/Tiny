// Represents a mutual exclusion lock for synchronization.
export class Mutex {
    // Acquires the lock, blocking if the lock is already held.
    fn lock(): null
    // Releases the lock.
    fn unlock(): null
}

// Creates and returns a new Mutex instance.
export fn mutex(): Mutex