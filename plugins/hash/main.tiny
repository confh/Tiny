import plugin "./hash" as hashPlugin;

export enum methods {
    md5
}

export fn hash(text: string, method: string): object {
    return hashPlugin.hash({
        text: text,
        method: method
    });
}