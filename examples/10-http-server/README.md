# 10 HTTP Server

This example shows `http.server`, route callbacks, request objects, query values, and JSON responses.

```bash
cd examples/10-http-server
../../tiny
```

Open:

- `http://localhost:8090/`
- `http://localhost:8090/echo?text=hello`
