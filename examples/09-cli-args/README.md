# 09 CLI Args

This example shows `process.args()` and a tiny command dispatcher. Run it with an explicit source file so arguments are passed to the script.

```bash
cd examples/09-cli-args
../../tiny src/main.tiny upper hello tiny
../../tiny src/main.tiny join one two three
```
