# 07 Errors

This example shows `try`, `catch`, `throw`, and the `error` standard module. It keeps the throw in the same file so the example stays focused on syntax.

```bash
cd examples/07-errors
../../tiny
```
