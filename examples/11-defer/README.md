# 11 Defer

This example demonstrates the `defer` keyword, which schedules a function call to be executed just before the current function returns. This is useful for cleanup tasks like closing files or finishing processes.

```bash
cd examples/11-defer
../../tiny
```
