# 17 SQLite

This example demonstrates using the built-in `sqlite` module to create a database, insert rows with parameterized queries, query data, update records, and close the connection.

```bash
cd examples/17-sqlite
../../tiny
```

Running it creates `data/app.db` with a `users` table and prints query results to the console.
