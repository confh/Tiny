# 02 Control Flow

This example shows `if`, `else`, `while`, classic `for`, `for in`, `break`, `continue`, ternary expressions, `match`, and enums.

```bash
cd examples/02-control-flow
../../tiny
```
