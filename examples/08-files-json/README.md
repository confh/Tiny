# 08 Files And JSON

This example shows reading a JSON file, parsing it, changing the object, and writing a new JSON file.

```bash
cd examples/08-files-json
../../tiny
```

Running it creates `data/output.json`.
